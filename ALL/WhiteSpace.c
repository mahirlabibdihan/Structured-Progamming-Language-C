// Input white-space characters are skipped, unless the specification includes a [, c, or n specifier

// Space, tab, line feed (newline), carriage return, form feed, and vertical tab characters are called "white-space characters" .

// whitespace characters in the format string of scanf instructs scanf to scan and discard all whitespace characters, if any, until the first non-whitespace character.